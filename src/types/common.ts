export interface PropsType {
  children?: React.ReactNode;
}

export interface JourneyType {
  title: string;
  subTitle: string;
  content: string;
  icon: string;
  date: string;
  organization:string;
}
